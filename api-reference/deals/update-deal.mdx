---
title: 'Update Deal'
api: 'PUT /api/v1/deals/{id}'
description: 'Update a single deal by ID'
---

## Path Parameters

<ParamField path="id" type="string" required>
  Deal UUID
</ParamField>

## Request Body

All fields are optional. Provide only the fields you want to update.

<ParamField body="title" type="string">
  Deal title
</ParamField>

<ParamField body="companyId" type="string">
  Parent company UUID
</ParamField>

<ParamField body="accountId" type="string">
  Linked account UUID
</ParamField>

<ParamField body="pipelineId" type="string">
  Pipeline ID
</ParamField>

<ParamField body="currentStageId" type="string">
  Stage ID
</ParamField>

<ParamField body="currentStageKey" type="string">
  Stage key
</ParamField>

<ParamField body="currentStageName" type="string">
  Stage display name
</ParamField>

<ParamField body="stageKey" type="string">
  Shorthand for current stage key
</ParamField>

<ParamField body="dealType" type="string">
  Deal type: `new_business`, `expansion`, `renewal`, `upsell`, `contraction`, `churn_save`
</ParamField>

<ParamField body="status" type="string">
  Deal status: `open`, `won`, `lost`, `cancelled`
</ParamField>

<ParamField body="ownerUserId" type="string">
  Deal owner UUID
</ParamField>

<ParamField body="amountCents" type="number">
  Deal amount in minor currency units
</ParamField>

<ParamField body="currency" type="string">
  Currency code
</ParamField>

<ParamField body="expectedCloseDate" type="string">
  Expected close date (ISO 8601)
</ParamField>

<ParamField body="closedAt" type="string">
  Actual close timestamp (ISO 8601)
</ParamField>

<ParamField body="metadata" type="object">
  Custom metadata
</ParamField>

<ParamField body="transitionReason" type="string">
  Audit note for stage changes
</ParamField>

<ParamField body="properties" type="object">
  Custom field values
</ParamField>

## Example Request

```bash
curl -X PUT https://api.arali.ai/api/v1/deals/DEAL_UUID \
  -H "Authorization: Api-Key YOUR_STATIC_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "currentStageId": "NEXT_STAGE_UUID",
    "status": "won",
    "closedAt": "2026-04-20T12:00:00.000Z",
    "transitionReason": "Closed won in CRM"
  }'
```
