---
title: 'Authentication'
description: 'Secure your API requests with JWT bearer tokens or static API keys'
---

## Overview

All Arali API endpoints require authentication via either JWT bearer tokens or static API keys. Pass credentials in the `Authorization` header of every request.

```bash
Authorization: Bearer <JWT>
Authorization: Api-Key <static_key>
```

## Authentication Context

- `Bearer <JWT>`: `enterpriseId` is read from JWT claims (`scopes` claim is optional).
- `Api-Key <static_key>`: `enterpriseId` and `scopes` are read from your public API key record.

## Available Scopes

Scopes control what actions your API credentials can perform. When creating a key, select only the scopes you need.

<AccordionGroup>
  <Accordion title="Companies" icon="building">
    | Scope | Permission |
    |-------|------------|
    | `companies:read` | List and view companies |
    | `companies:write` | Create and update companies |
    | `companies:delete` | Delete companies |
  </Accordion>
  
  <Accordion title="Accounts" icon="briefcase">
    | Scope | Permission |
    |-------|------------|
    | `accounts:read` | List and view accounts |
    | `accounts:write` | Create and update accounts |
    | `accounts:delete` | Delete accounts |
  </Accordion>
  
  <Accordion title="Contacts" icon="users">
    | Scope | Permission |
    |-------|------------|
    | `contacts:read` | List and view contacts |
    | `contacts:write` | Create and update contacts |
    | `contacts:delete` | Delete contacts |
  </Accordion>

  <Accordion title="Deals" icon="handshake">
    | Scope | Permission |
    |-------|------------|
    | `deals:read` | List and view deals |
    | `deals:write` | Create and update deals |
    | `deals:delete` | Delete deals |
  </Accordion>
  
  <Accordion title="Calls" icon="phone">
    | Scope | Permission |
    |-------|------------|
    | `calls:read` | List and view call records |
    | `calls:write` | Create call records |
  </Accordion>
  
  <Accordion title="Meetings" icon="calendar">
    | Scope | Permission |
    |-------|------------|
    | `meetings:read` | List and view meetings |
    | `meetings:write` | Create and update meetings |
    | `meetings:delete` | Delete/cancel meetings |
  </Accordion>

  <Accordion title="Associations" icon="link">
    | Scope | Permission |
    |-------|------------|
    | `associations:write` | Create contact-company/account associations |
  </Accordion>

  <Accordion title="Interactions" icon="arrows-rotate">
    | Scope | Permission |
    |-------|------------|
    | `interactions:write` | Link interactions to contacts and deals |
  </Accordion>
  
  <Accordion title="Fields" icon="sliders">
    | Scope | Permission |
    |-------|------------|
    | `fields:read` | List field definitions and values |
    | `fields:write` | Create field definitions and set values |
    | `fields:delete` | Delete field definitions |
  </Accordion>

  <Accordion title="Pipelines & Stages" icon="diagram-project">
    | Scope | Permission |
    |-------|------------|
    | `pipelines:read` | List and view pipelines |
    | `pipelines:write` | Create and update pipelines |
    | `pipelines:delete` | Delete pipelines |
    | `stages:read` | List and view pipeline stages |
    | `stages:write` | Create and update pipeline stages |
    | `stages:delete` | Delete pipeline stages |
  </Accordion>

  <Accordion title="Products & Product Lines" icon="box">
    | Scope | Permission |
    |-------|------------|
    | `products:read` | List and view products |
    | `products:write` | Create and update products |
    | `products:delete` | Delete products |
    | `product-lines:read` | List and view product lines |
    | `product-lines:write` | Create and update product lines |
    | `product-lines:delete` | Delete product lines |
  </Accordion>
  
  <Accordion title="Plans" icon="clipboard-list">
    | Scope | Permission |
    |-------|------------|
    | `plans:read` | List and view plans |
    | `plans:write` | Create and update plans |
    | `plans:delete` | Delete plans |
  </Accordion>
  
  <Accordion title="Subscriptions" icon="rotate">
    | Scope | Permission |
    |-------|------------|
    | `subscriptions:read` | List and view subscriptions |
    | `subscriptions:write` | Create and update subscriptions |
    | `subscriptions:delete` | Delete subscriptions |
  </Accordion>
  
  <Accordion title="Invoices" icon="file-invoice">
    | Scope | Permission |
    |-------|------------|
    | `invoices:read` | List and view invoices |
    | `invoices:write` | Create and update invoices |
    | `invoices:delete` | Delete invoices |
  </Accordion>
  
  <Accordion title="Usage" icon="chart-line">
    | Scope | Permission |
    |-------|------------|
    | `usage:read` | Query usage data |
    | `usage:write` | Report usage data |
  </Accordion>
  
  <Accordion title="Key Role Assignments" icon="user-shield">
    | Scope | Permission |
    |-------|------------|
    | `key_roles:read` | List and view role assignments |
    | `key_roles:write` | Create and update role assignments |
    | `key_roles:delete` | Delete role assignments |
  </Accordion>
  
  <Accordion title="Users" icon="user">
    | Scope | Permission |
    |-------|------------|
    | `users:read` | List and view users |
  </Accordion>
  
  <Accordion title="Action Items" icon="check-circle">
    | Scope | Permission |
    |-------|------------|
    | `action-items:read` | List and view action items |
    | `action-items:write` | Create, update, and delete action items |
  </Accordion>
  
  <Accordion title="Notes" icon="note-sticky">
    | Scope | Permission |
    |-------|------------|
    | `notes:read` | List and view notes |
    | `notes:write` | Create, update, and delete notes |
  </Accordion>

  <Accordion title="Metric Engine" icon="chart-bar">
    | Scope | Permission |
    |-------|------------|
    | `metrics:definitions:read` | List and view metric definitions |
    | `metrics:definitions:write` | Create and update metric definitions |
    | `metrics:definitions:delete` | Delete metric definitions |
    | `metrics:computations:read` | Query metric computations |
    | `metrics:computations:write` | Ingest metric computations |
    | `metrics:events:read` | Query product events |
    | `metrics:events:write` | Ingest product events |
    | `metrics:aggregates:read` | Query metric aggregates |
  </Accordion>
</AccordionGroup>

## Error Responses

### 401 Unauthorized

Returned when credentials are missing or invalid.

```json
{
  "error": "Invalid token"
}
```

<Warning>
  If you receive a 401 error, check that:
  - Your credentials are correctly formatted
  - The API key has not expired or been revoked
  - There are no extra spaces or characters
</Warning>

### 403 Forbidden

Returned when the credential lacks the required scope for the operation.

```json
{
  "error": "Insufficient permissions. Required scope: companies:write"
}
```

<Tip>
  If you receive a 403 error, you'll need to generate a new API key with the required scopes.
</Tip>

## Security Best Practices

<CardGroup cols={2}>
  <Card title="Keep Credentials Secret" icon="shield">
    Never expose API keys or JWTs in client-side code, public repositories, or logs.
  </Card>
  <Card title="Use Minimal Scopes" icon="minimize">
    Only request the scopes you actually need. This limits potential damage if a key is compromised.
  </Card>
  <Card title="Rotate Regularly" icon="rotate">
    Rotate API keys periodically, especially if team members leave or credentials may have been exposed.
  </Card>
  <Card title="Use Environment Variables" icon="terminal">
    Store API keys and JWTs in environment variables rather than hardcoding them in your application.
  </Card>
</CardGroup>

## Example: Validate Authentication

```bash
# JWT
curl -X GET 'https://api.arali.ai/api/v1/companies' \
  -H 'Authorization: Bearer YOUR_JWT'

# Static API Key
curl -X GET 'https://api.arali.ai/api/v1/companies' \
  -H 'Authorization: Api-Key YOUR_STATIC_KEY'
```

A successful response indicates your credentials are valid and include the `companies:read` scope.
